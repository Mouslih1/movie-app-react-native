import * as React from 'react';
import {Text, View, StyleSheet} from 'react-native';

const TicketScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Ticket scren</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default TicketScreen;
